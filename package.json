{
  "name": "@pocketshell/core",
  "version": "0.1.0",
  "description": "PocketShell's shared contract layer: sync, aplexer, agent launch, SSH config, OSC 52 — one implementation for the desktop, web, and Android clients.",
  "license": "MIT",
  "author": "Alexey Grigorev <alexey.s.grigoriev@gmail.com>",
  "type": "module",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/fix-esm-extensions.mjs && tsc -p tsconfig.cjs.json && echo '{\"type\":\"commonjs\"}' > dist/cjs/package.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "embed": "node scripts/build-embed.mjs && node scripts/verify-embed.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "quickjs-emscripten": "^0.32.0",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
